&НаСервере
Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	// Объединяем строки с одинаковой номенклатурой и ценой, суммируя количество и сумму
	ТабНоменклатура.Свернуть("Номенклатура, Цена", "Количество, Сумма");
КонецПроцедуры